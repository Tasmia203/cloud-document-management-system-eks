apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "cloud-file-manager.fullname" . }}-config

data:
  BUCKET_NAME: {{ .Values.aws.bucketName | quote }}
  AWS_DEFAULT_REGION: {{ .Values.aws.region | quote }}